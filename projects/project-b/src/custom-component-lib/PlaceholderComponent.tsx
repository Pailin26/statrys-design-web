import React from "react";
import { Button } from "@statrys/web-ds";

/**
 * Custom Component Lib — components specific to Product B. Check
 * project-a's custom-component-lib first before building something new;
 * if it's the same need, promote it into packages/web-ds instead.
 */
export function PlaceholderComponent() {
  return (
    <div>
      <Button variant="primary">Placeholder</Button>
    </div>
  );
}
